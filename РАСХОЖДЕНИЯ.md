# Расхождения между таблицей и формулами

Всё, что ниже, найдено при переносе листа **«Мидзуки Сахароза»**. В режиме
`Config(excel_compat=True)` проект воспроизводит таблицу **точно** (0.000000 %),
включая перечисленные особенности. В режиме `excel_compat=False` считается по
формулам.

Первые два пункта меняют числа заметно, остальные — мелочи, но их стоит
проверить.

---

## 1. Бонус базового урона учтён дважды (влияет сильно)

Строка **«EMM»** в таблице:

```
EMM = (1 + 6·МС/(МС+2000) + бонусы) × (1 + СУММ(B16:E16))
```

Строка **урона реакции**:

```
= 1446.85 × 0.75 × (1 + СУММ(B16:E16)) × Крит × Сопр. × (1 + Возвышение) × EMM
                    ^^^^^^^^^^^^^^^^^^                                     ^^^
```

Множитель `(1 + Base stellar swirl DMG%)` = `1 + 0.07 + 0.14` = `1.21`
входит и в `EMM`, и отдельно — то есть возводится в квадрат. По формуле
с картинки он применяется один раз.

**Цена вопроса:** урон всех реакций завышен на 21 %, то есть по факту
**−17.3 % урона реакций** при переходе к правильной формуле:

| Отряд | DPR (как в таблице) | DPR (по формуле) | Δ |
|---|---:|---:|---:|
| Блок 1 (C0) | 2 660 789 | 2 611 422 | −1.9 % |
| Блок 2 (C1) | 2 757 490 | 2 709 897 | −1.7 % |
| Блок 3 (C1, др. оружие) | 3 198 408 | 3 144 547 | −1.7 % |
| Блок 4 (C2) | 3 111 424 | 3 077 875 | −1.1 %\* |

\* в блоке 4 к этому добавлены исправления из пунктов 5 и 6.

Обратите внимание: в прямом уроне звёздного рассеивания (строка «Total SSW DMG»)
двойного учёта нет — там `EMM` умножается один раз, и это правильно.

---

## 2. Урон вихря не попадает в DPR

`DPR = СУММ(B49:E49)`, где каждый столбец — `Total Elemental + Total SSW + Total SSW reaction Anemo`.
Строка **«Total SSW reaction Cryo DMG»** (детонация вихря, 251–273 тыс. урона)
ни в один из этих столбцов не входит и, соответственно, в DPR и DPS не попадает.

Похоже на потерянную ссылку: урон вихря есть, но в итог не идёт. В проекте он
включён в `TeamResult.dpr`, поэтому при сверке эти величины разделены
(`scripts/compare_with_excel.py` сравнивает «DPR без вихря» и «урон вихря» отдельно).

Если это было сделано намеренно — уберите события вихря из `reactions()` в пресете.

---

## 3. У Одетты на поле не учитывается A4 Сахарозы

```
D68 = D67 + 120 + 50 + 0.2 * F67
                             ^^^  пустая ячейка (должно быть E67)
```

Из-за этого «Total EM on field instructor + Sucrose» у Одетты = 578 вместо 787.2.
Ячейка используется в строке «Total SSW DMG» (особый навык), так что на итог влияет.
Есть во всех четырёх блоках.

Воспроизводится баффом `EXCEL-баг: ссылка на пустую ячейку вместо МС Сахарозы`.

---

## 4. У Одетты вне поля пропало слагаемое +60 %

В скобке «EMM»:

| | на поле | вне поля |
|---|---|---|
| Мидзуки | `… + 50 % + 20 % + 60 %` | — |
| Крио ГГ | `… + 50 % + 60 %` | `… + 50 % + 60 %` |
| Сахароза | — | `… + 50 % + 60 %` |
| **Одетта** | `… + 50 % + 60 %` | `… + 50 %` ← нет 60 % |

Либо опечатка, либо особенность персонажа. Воспроизводится явным баффом
`−60 % при requires=(off_field,)` у Одетты — если это опечатка, просто удалите его
из `odette_build()`.

---

## 5. Блок 4 (C2 Мидзуки): снижение сопротивления применено не везде

Сами числа в таблице **верные**. C2 снимает 20 % сопротивления, и множители
раскладываются точно:

| стихия | сопротивление цели | множитель |
|---|---:|---:|
| анемо, без C2 | +10 % | 0.90 |
| анемо, с C2 | −10 % | **1.05** |
| крио, без C2 (уже с −40 % от Изумрудной тени) | −30 % | 1.15 |
| крио, с C2 | −50 % | **1.25** |

Разный «прирост» множителя (+0.15 против +0.10) — просто излом формулы
сопротивления в нуле: ниже нуля сопротивление работает вполсилы.

Проблема в другом: `RES Cryo with C2 Midzuki` = 1.25 подставлен **только**
в детонацию вихря и в строку «Навык — SSW» Одетты. Урон Крио ГГ, особый навык
Одетты и все элементальные строки считаются по 1.15, хотя эффект C2 действует
на весь крио-урон внутри состояния «Дрейф грёз».

В режиме совместимости это воспроизведено точечно. В обычном режиме
сопротивление стало обычным статом: у цели задано базовое сопротивление
(`Enemy.res`), а Изумрудная тень и C2 снимают его баффами — C2 с условием
`requires=("dreamdrifter",)`. Итог блока 4: **3 077 875** вместо 3 111 424.

---

## 6. Бонус урона от C2 Мидзуки достаётся не тем персонажам

Формулировка C2: пока Мидзуки в состоянии «Дрейф грёз», каждая единица её МС
даёт ближайшим членам отряда +0.04 % бонуса **пиро, гидро, крио и электро**
урона. Анемо в списке нет.

В таблице:

| персонаж | стихия | должен получать | в таблице |
|---|---|---|---|
| Мидзуки | анемо | нет | **да** (`B131 = 32 % + 0.04 % × B123`) |
| Крио ГГ | крио | да | **нет** (строки `C131` вообще нет) |
| Одетта | крио | да | да (`D131`) |

То есть бонус ушёл ровно не туда. Правильно он влияет только на обычный
элементальный урон Крио ГГ и Одетты: прямой урон звёздного рассеивания и урон
реакции обычный бонус урона не берут (у них своя скобка).

Для Крио ГГ это +41.8 % к строке «Навык (АТК)», для Одетты — +55.4 %;
у Мидзуки, наоборот, +55.4 % убирается.

---

## 7. C1 Мидзуки: слагаемое «+550 % × 3» без стата

```
B102 = (B63*(B86+B87) + (B67*B85 + B68*B85*3 + 550%*3)) * …
                                               ^^^^^^^^
```

Все соседние слагаемые — «стат × множитель», а это просто число `16.5`.
Похоже, потерян множитель (скорее всего должно быть `МС × 550 % × 3`).
На итог не влияет (16.5 против ~1.4 млн), но если стат вернуть — прибавка будет
порядка 22 тыс. урона.

Воспроизведено как плоское слагаемое в источнике `C1: доп. базовый урон`.

---

## 8. Мелочи

* **Крит Крио ГГ** считается как `CV − 1`, у остальных персонажей — `1 + CV²/8`.
  Скорее всего опечатка: `1 + 4.328²/8 = 3.341` против `3.328`, разница ~0.4 %.
  Режим задаётся полем `crit_mode` у персонажа.
* **Формула крита `1 + CV²/8`** — приближение «оптимального распределения»
  шанса и урона крита. Оно даёт верный ответ только при `2 × шанс = крит. урон`.
  Если нужен обычный расчёт — `crit_mode="expected"` и заполните
  `crit_rate` / `crit_dmg` вместо `crit_value`.
* **Блок 1** озаглавлен «Мидзуки C0», хотя по описанию отрядов должен быть C1.
  Плюс у Мидзуки там есть лишние `+120` МС, которых нет в остальных блоках.
  Оставлено как в таблице (`доп. +120 МС только в первом блоке листа`).
* **Строка «EMM on field» у Сахарозы** (E17) в таблице нигде не используется,
  причём считается по её МС «вне поля». У неё нет отдельной строки МС для окна
  Инструктора, поэтому прибавка «+10 % МС Мидзуки» считается по МС Мидзуки
  без Инструктора. Воспроизведено отдельным баффом.
* **+120 МС от Инструктора** учтено только в строках «on field»; в строках
  «not on field instructor» его нет. Проверьте, так ли задумано.
